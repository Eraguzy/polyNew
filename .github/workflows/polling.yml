name: keepalive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: curl keepalive
        run: curl -fsS https://sophisticated-aloisia-isabellesdesk-6e0ebfef.koyeb.app/keepalive
